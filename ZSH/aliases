alias findps="ps -ax | grep"

# exa is the new ls 
alias ls="eza"
alias l="ls -h -l"
alias cl="clear; l"

# using builtin time instead of zsh's
alias time='\time -p'
 
# Change dir, clear screen and list files
c() {
	cd $1 ; cl
}
 
# Change dir and list files
cdl() {
	cd $1 ; l
}
 
# jump to dir, clear screen and list files
jl() {
	j $1 ; cl
}

############## PPRO ALIASES #############

alias mci="mvn clean install"
alias mci-skip-tests="mvn clean install -DskipTests=true"
alias mci-sonar="mvn clean install sonar:sonar"
alias mci-sonar-skip-tests="mvn clean install -Dmaven.test.skip=true sonar:sonar"

alias kb="kubectl"

alias ruby3=/usr/local/opt/ruby

